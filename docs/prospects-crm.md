# Internal prospects CRM (admin)

Collection: `la_mesa_prospects`. Admin UI: `/admin/prospects`. Cold send: Templates → custom Cold Mail (batch 50, status `to_contact` → `contacted`).

Email required. Import: Google Sheet URL (public) or CSV paste. Dedup/merge by email. No live Database Perso bridge for cold outreach.
